import { Mail, Plus, ShieldCheck, Stethoscope, UserRoundCheck } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { getDoctors } from "@/lib/data/workspace";

const colors = ["bg-emerald-100 text-emerald-800", "bg-blue-100 text-blue-800", "bg-violet-100 text-violet-800", "bg-amber-100 text-amber-800"];
export default async function CareTeamPage() { const doctors = await getDoctors(); return <AppShell active="Care team"><div className="mx-auto max-w-[1300px]"><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-1 text-sm font-medium text-[#167d63]">Clinical workspace</p><h1 className="text-2xl font-bold tracking-tight text-[#17362d] md:text-[30px]">Care team</h1><p className="mt-1 text-sm text-[#70817b]">Clinicians available for follow-up and escalation.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#167d63] px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17}/>Add clinician</button></div><div className="mb-5 flex items-center gap-3 rounded-2xl border border-emerald-200 bg-emerald-50/70 p-4 text-sm text-emerald-900"><ShieldCheck className="shrink-0" size={21}/><p><strong>{doctors.filter((item) => item.active).length} clinicians active.</strong> Calls requiring review are routed to human staff.</p></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{doctors.map((doctor, index) => <article key={doctor.id} className="rounded-2xl border border-[#dfe8e4] bg-white p-6 shadow-[0_2px_10px_rgba(23,54,45,.035)]"><div className="flex items-start justify-between"><span className={`grid h-12 w-12 place-items-center rounded-2xl text-sm font-bold ${colors[index % colors.length]}`}>{doctor.name.split(" ").filter((part) => part !== "Dr.").slice(0,2).map((part) => part[0]).join("")}</span><span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700"><span className="h-1.5 w-1.5 rounded-full bg-emerald-500"/>Active</span></div><h2 className="mt-5 font-bold text-[#29473e]">{doctor.name}</h2><p className="mt-1 flex items-center gap-1.5 text-sm text-[#167d63]"><Stethoscope size={15}/>{doctor.specialty}</p><p className="mt-4 flex items-center gap-2 text-xs text-[#7c8c86]"><Mail size={14}/>{doctor.email}</p><div className="mt-5 border-t border-[#e8eeeb] pt-4"><p className="flex items-center gap-2 text-xs font-medium text-[#62756e]"><UserRoundCheck size={15}/>Available for clinical review</p></div></article>)}</section></div></AppShell>; }
